Module mMains

    Public Sub Main_Setup()
        Dim str_status = String.Empty
        Dim delay As Integer = 200
        Dim progress As Integer = 0

        _frmSplash.Show()
        _frmSplash.setProgress("Done", 100)

    End Sub

End Module
